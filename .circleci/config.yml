version: 2.1

orbs:
  android: circleci/android@2.0.3

jobs:
  build:
    machine:
      image: android:2024.11.1
    steps:
      - checkout
      - run:
          name: Build APK
          command: ./gradlew assembleRelease

workflows:
  build_apk:
    jobs:
      - build
